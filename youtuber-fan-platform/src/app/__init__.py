"""The application package.

Deliberately named ``app`` and not after the product: manifest §7 asks that the
product name appear in exactly one place, so renaming costs one line. That place
is ``settings.product.name``.
"""
